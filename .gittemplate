# <제목>------------제목은 50자 이내로 작성 --------------|

# ----- 본문은 70자 이내로 작성 ----------------------------------------------|

# 적용 전 아래 부분은 지워서 사용할 것!
# 해당 파일을 템플릿으로 설정. 경로는 절대경로로 할 것!
#   git config --global commit.template ~/.gittemplate
#
# 빔을 커밋 메시지 작성 에디터로 설정 하지만 AutocomplPop은 비활성화
#   git config --global core.editor 'vim +AcpDisable'
#
# 커밋시 사용할 유저 정보 등록
#   git config --global user.name 'MaetDol'
#   git config --global user.email 'email@gmail.com'
# 
# 특정 레포지토리에서 유저네임 입력없이 바로 비밀번호만 묻게 할때 사용. 해당 레포 폴더에서 사용할 것
#   git remote set-url origin https://{Username}@github.com/{Username}/{Repository name}.git
#
# 또는, PAT(Personal Access Token)을 사용할 것(한번만 하면 되니 편하다! 하지만 평문으로 저장되는데..?)
# Github -> Settings -> Developer settings -> Personal Access Token 에서 토큰 발급
#   git config --global credential.helper store # 윈도우는 store 대신 manager
# 한 다음, 푸시할때 비밀번호 대신 토큰을 넣어주면 그 이후로 묻지 않는다
